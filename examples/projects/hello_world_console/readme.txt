The Ant Build Type property for the Release confirugation of the Android
platform is set to Debug rather than Release so that the apk will be signed with
a debug key.  Setting the property to Release requires additional configuration
or deployment will fail.
